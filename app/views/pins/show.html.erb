<div id='pin' >
  <h2><%= @pin.name %></h2>
  <%= image_tag(@pin.image.normal.url)%>
</div>
<%= link_to "Edit Pin", [:edit, @board, @pin ] %>
<%= link_to "Delete Pin", [@board, @pin], method: :delete, data: { confirm: "Are you sure you want to delete this pin?" } %>
